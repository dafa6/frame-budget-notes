# Second Unit Notes

Static site: notes on the parts of a generative video prompt that are direction rather
than specification.

- `static/` — the built HTML, no build step required
- `main.ts` — Deno static file server used by Deno Deploy

Sources and the generator live elsewhere; this repository holds the published output only.
